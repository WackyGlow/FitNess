---
apiVersion: v1
kind: Service
metadata:
  name: fitness
spec:
  type: LoadBalancer
  selector:
    app: fitness
  ports:
  - port: 80
    targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fitness
  labels:
    app: fitness
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fitness
  template:
    metadata:
      labels:
        app: fitness
    spec:
      containers:
      - name: userservice
        image: us-central1-docker.pkg.dev/GOOGLE_PROJECT/fitness/userservice:latest
            
        

